syntax = "proto2";

option java_outer_classname = "UserListSP";

message data{
    required int32 proto_type = 1;
    required int64 proto_id = 2;
    required int64 utc_time = 3;

    repeated string users = 4; //Subject to change, strings are a poor representation, but it works for now

}