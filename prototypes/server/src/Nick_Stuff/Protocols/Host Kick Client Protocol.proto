syntax = "proto2";

option java_outer_classname = "HKickCP";

message data{
    required int32 proto_type = 1;
    required int64 proto_id = 2;
    required int64 utc_time = 3;

    required string user = 4; //I think the only identification the hosts needs access to is the usernames of the clients

}